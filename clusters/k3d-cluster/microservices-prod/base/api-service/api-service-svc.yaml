apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: microservices-prod
spec:
  selector:
    app: api-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
