apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: microservices-prod
spec:
  selector:
    app: frontend-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
