apiVersion: v1
kind: Service
metadata:
  name: identity
  namespace: microservices-prod
spec:
  selector:
    app: identity-provider
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
